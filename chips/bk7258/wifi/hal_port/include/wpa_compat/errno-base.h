/* Armino's bk_patch/errno-base.h supplies four standard errno values. NuttX
 * owns the canonical definitions, so retain the upstream include name while
 * avoiding a fragile path to an unvendored bk_patch directory. */

#ifndef __BK7258_WIFI_WPA_COMPAT_ERRNO_BASE_H
#define __BK7258_WIFI_WPA_COMPAT_ERRNO_BASE_H

#include <errno.h>

#endif /* __BK7258_WIFI_WPA_COMPAT_ERRNO_BASE_H */
